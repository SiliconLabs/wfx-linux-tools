#!/bin/bash
# Copyright (c) 2018, Silicon Laboratories
# See COPYING for license.

. wfx_set_env
check_root

case "$(wfx_bus)" in
    spi)
        wfx_spi_reload "$@"
        ;;
    sdio)
        wfx_sdio_reload "$@"
        ;;
    *)
        exit 1
        ;;
esac
