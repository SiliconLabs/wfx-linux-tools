#!/bin/bash

. wfx_set_env
. wfx_colors

if [ -f "${1}" ];
then
	set -ex
	pds_compress ${1} /sys/kernel/debug/ieee80211/phy*/wfx/send_pds
	set +ex
else
	printf "${LRED}No input file for 'send_pds'!${NO}\n"
fi
